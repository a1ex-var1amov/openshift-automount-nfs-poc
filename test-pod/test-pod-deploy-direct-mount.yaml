apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-pod-direct-mount
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-pod-direct-mount
  template:
    metadata:
      labels:
        app: test-pod-direct-mount
    spec:
      securityContext:
        runAsUser: 2002
        runAsGroup: 2002
        supplementalGroups: [2002]
      initContainers:
      - name: nfs-mounter
        image: quay.io/a13x22/toolbox-container:basic
        imagePullPolicy: Always
        securityContext:
          runAsUser: 0
          runAsGroup: 0
        command: ["/bin/bash", "-c"]
        args:
        - |
          # Install NFS utilities
          dnf install -y nfs-utils
          
          # Create mount points
          mkdir -p /shared/home
          mkdir -p /shared/home-individual
          
          # Mount NFS directly with proper SELinux context
          mount -t nfs -o rw,vers=3,context=system_u:object_r:user_home_t:s0 172.30.240.59:/exports/home /shared/home
          
          # Set proper permissions
          chmod 755 /shared/home
          chown 2002:2002 /shared/home
          
          # Create individual symlinks for each user directory
          for user in usera userb userc userd usere xyz; do
            if [ -d "/shared/home/$user" ]; then
              ln -sf "/shared/home/$user" "/shared/home-individual/$user"
              chown -h 2002:2002 "/shared/home-individual/$user"
            fi
          done
          
          # Set permissions on the individual directory
          chmod 755 /shared/home-individual
          chown 2002:2002 /shared/home-individual
        volumeMounts:
        - name: shared-home
          mountPath: /shared/home
        - name: shared-home-individual
          mountPath: /shared/home-individual
      containers:
      - name: test-pod
        image: quay.io/a13x22/toolbox-container:basic
        imagePullPolicy: Always
        workingDir: /tmp
        command: ["/bin/bash","-lc"]
        args:
        - |
          id
          echo "=== Testing direct NFS mount approach ==="
          echo "=== Testing /home directory access ==="
          ls -la /home
          echo "=== Testing individual home access ==="
          ls -la /home-individual/
          echo "=== Testing direct access to user directories ==="
          ls -la /home/userb
          ls -la /home/xyz
          echo "=== Testing file operations ==="
          echo $(date) hello from direct-mount:$HOSTNAME >> /home/userb/hello.txt
          echo $(date) hello shared from direct-mount:$HOSTNAME >> /home/xyz/hello.txt
          echo "=== Testing write permissions ==="
          echo "test write" > /home/userb/test-write.txt
          ls -la /home/userb/test-write.txt
          sleep infinity
        volumeMounts:
        - name: shared-home
          mountPath: /home
        - name: shared-home-individual
          mountPath: /home-individual
      serviceAccountName: test-pod
      volumes:
      - name: shared-home
        emptyDir: {}
      - name: shared-home-individual
        emptyDir: {}
